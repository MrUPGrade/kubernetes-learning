apiVersion: "v1"
kind: "PersistentVolume"
metadata:
  name: kta-log-v
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    # directory location on host
    path: /data/kta-log-v/
    # this field is optional
    type: DirectoryOrCreate

